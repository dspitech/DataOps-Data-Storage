dataops_dbt:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: dataops
      schema: main
      schemas_and_paths:
        main: "/opt/airflow/project/database/dataops.db"
      schema_directory: "/opt/airflow/project/database"
